<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.htsc.zyz.listner.mapper.TestMapper">

   <select id="queryCount" resultType="java.lang.Integer">
       select count(1)
       from socre
   </select>

</mapper>